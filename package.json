{
  "name": "@ava-avant-iconic/discord-webhook-helper",
  "version": "1.0.0",
  "description": "TypeDiscord webhook helper library with embed builders, action rows, file uploads, retry logic, and rate limiting awareness",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test dist/*.test.js",
    "prepublishOnly": "npm run build && npm test"
  },
  "keywords": [
    "discord",
    "webhook",
    "embed",
    "bot",
    "automation"
  ],
  "author": "AVA <ava@avant-iconic.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/ava-avant-iconic/discord-webhook-helper.git"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
